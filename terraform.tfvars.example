# Copy this file to terraform.tfvars and fill in your values
# cp terraform.tfvars.example terraform.tfvars

aws_region    = "us-east-1"
instance_type = "t3.xlarge"     # 4 vCPU, 16GB RAM — recommended for building from source
key_name      = "my-keypair"    # REQUIRED: name of your existing EC2 Key Pair
admin_email   = "admin@example.com"

# Restrict SSH to your IP for security (recommended)
# Find your IP: curl -s ifconfig.me
# allowed_ssh_cidrs = ["203.0.113.42/32"]

# Disk size in GB (Overleaf + TexLive + Docker images need ~30GB)
# root_volume_size = 50
